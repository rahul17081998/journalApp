package com.rahul.journal_app.enums;

public enum PrivacyLevel {
    PRIVATE,   // Only the author can view
    SHARED,    // Only author and specifically invited users can view
    PUBLIC     // All users of the platform can view
}
